## What this changes

<!-- One or two sentences. Link the issue, for example: Closes #12 -->

## How it was tested

- Windows version:
- Wallpaper app (none, Wallpaper Engine, Lively, other):
- Monitors and scaling:

<!-- What you did to check the change. For desktop changes, list the steps from docs/en/manual-test.md you ran. -->

## Checklist

- [ ] `./build.ps1` and `./test.ps1` pass.
- [ ] New interface text is in every file in `src/Orla/Strings`.
- [ ] Nothing slow runs on the UI thread (file system, shell or network calls).
- [ ] The user guides in `docs/pt-BR` and `docs/en` are updated if behaviour changed.
- [ ] Screenshots were made with `Orla.exe --render` or show no personal paths, user names or files.
